import { Dimensions } from "react-native"

const {width,height} = Dimensions.get('window')
export const theme = {
    background : '#fff' ,
    title : '#fff',
    text : 'red',
    bannerHeight : height *0.5,
    width : width,
    height : height
}

